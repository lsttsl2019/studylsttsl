<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lsttsl.study_01">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".util.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Study_01">
        <activity android:name=".activtiy.PlayerActivity"></activity>
        <activity android:name=".login.SignUpActivity" />
        <activity android:name=".login.PwSearchActivity" />
        <activity android:name=".login.LogingPageActivity" />
        <activity android:name=".login.LoginMainPageActivity" />
        <activity
            android:name=".activtiy.IntroActivity"
            android:theme="@style/IntroTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activtiy.MainActivity" />
    </application>

</manifest>